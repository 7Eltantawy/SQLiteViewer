import 'package:flutter/material.dart';

class DBTableCard extends StatelessWidget {
  const DBTableCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
